The server creates an HTTP server but does not attach Socket.io in server/_core/index.ts. Messages.tsx is still a static mock UI: hardcoded chats/messages and console.log on send, with no tRPC or useWebSocket calls. A safe slice is to preserve existing persistence procedures, add Socket.io server registration with authenticated room/message handlers, then wire Messages incrementally with a persisted fallback and tests.
