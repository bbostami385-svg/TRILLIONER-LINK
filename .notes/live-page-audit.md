LiveStreaming.tsx currently seeds two hardcoded streams, uses a placeholder player, stores chat only in local state, and does not call tRPC. A safe next step is to query liveStream.getActiveLiveStreams, load getStreamDetails/getStreamChatMessages for a selected stream, render an HTML video element when hlsUrl exists, call addStreamViewer on selection, and use protected sendStreamChatMessage with a persisted fallback. Provider-specific HLS playback remains separate from native video support.
